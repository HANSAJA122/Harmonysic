import React from 'react';
import ProfileView from '@/components/Profile/ProfileView';

export const metadata = {
  title: 'Profile | Harmonysic',
};

export default function ProfilePage() {
  return <ProfileView />;
}
